## Ansible Playbook for K8s

This playbook can be used to install kubernetes on a local virtual machine, vagrant box and on prem-servers.

The following command creates an initial folder structure\
``
Ansible-galaxy init <role-name>
``

